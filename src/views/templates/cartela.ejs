<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF com Conteúdo em Páginas Alternadas</title>

    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        .folha {
            /* (210mm x 297mm) */
            box-sizing: border-box;
            width: 209.9mm;
            height: 296.86mm;
            display: block;
            background-color: gray;
            page-break-inside: avoid;
            page-break-after: always;
            position: relative;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .conteudo {
            color: white;
            text-align: center;
            padding: 20px;
            background-image: url('<%= background %>');
            background-position: center;
            background-size: 100% 100%;
            height: 296.86mm;
            position: relative;
        }

        .bingoContainer {
            position: absolute;
            top: 25%;
            left: 0%;
            width: 100%;
            margin: auto;
            display: grid;
            gap: 10mm;
            grid-template-columns: repeat(4, 1fr);
            z-index: 99;
            align-items: center;
            justify-content: center;
        }

        .bingoContainer img {
            width: 80mm;
            height: auto;
            margin: auto;
            padding: 5mm;
            border: 2px solid blue;
        }

        .pageNumber {
            position: absolute;
            top: 10mm;
            right: 10mm;
            background-color: red;
            color: white;
            z-index: 999;
            font-size: 20px;
        }

        /* Estilos específicos para a grid 4x6 */
        .bingoGrid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            margin-top: 20px;
        }

        .bingoGrid p {
            border: 2px solid;
            padding: 10px;
            text-align: center;
        }
    </style>
</head>

<body>
    <!-- container -->
    <section class="container">
        <% bingos.forEach(element => { %>
        <!-- conteúdo -->
        <div class="folha">
            <p class="pageNumber"><%= element.number %></p>
            <div class="conteudo">
                <h1>Conteúdo da Página 1</h1>
                <p>Seu conteúdo vai aqui...</p>
                <div class="bingoContainer">
                    <div class="bingoGrid">
                        <p>B</p>
                        <p>I</p>
                        <p>N</p>
                        <p>G</p>
                        <p>O</p>
                        <% for (let j = 0; j < element.bingo.length; j++) { %>
                        <p><%= element.bingo[j] %></p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <% }) %>
    </section>
</body>

</html>
